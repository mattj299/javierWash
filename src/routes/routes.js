import { domainRoute } from "./domainRoute";

export const HOME = `${domainRoute}/`;
export const ABOUT = `${domainRoute}/about`;
export const SERVICES_AND_PRICING = `${domainRoute}/services_pricing`;
export const CONTACTS = `${domainRoute}/contacts`;
export const FAQ = `${domainRoute}/faq`;
